
  # Document generation tool

  This is a code bundle for Document generation tool. The original project is available at https://www.figma.com/design/gJOqcw7BkkqceJr6ifNfGx/Document-generation-tool.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  